import colours from './colours';
import spacing from './spacing';
import typography from './typography';
import paperTheme from './paperTheme';

export { colours, spacing, typography, paperTheme };
export { default } from './paperTheme';

export const theme = {
  colours,
  spacing,
  typography,
  paperTheme,
};